package com.tan.seminario.backend.CasosDeUsos.Seguridad.ABMEmpleado.DTOs.AltaEmpleado;

import lombok.Builder;
import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
@Builder
public class AltaEmpleadoResponse {

    // Devolver el usuario (email, password)
    // Devolver el empleado (codEmpleado, nombre, apellido, dni, fechaNacimiento, telefono, direccion, activo, rol)
    // Mensaje el empleado fue creado con exito.
}
